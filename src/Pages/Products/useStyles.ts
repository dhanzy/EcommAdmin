import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    productListImg: {
        height: '3rem',
        width: '3rem',
        borderRadius: '50%',
        marginRight: "20px",
        objectFit: 'cover',
    },
}));

export default useStyles;